package com.atompacman.atomlog;

import com.atompacman.configuana.param.StrictParam;

public enum Parameters implements StrictParam {
	CONSOLE_VERBOSE,
	WRITE_LOG_FILE,
	LOG_FILE_VERBOSE,
	LOG_DIRECTORY,
	APPEND_DATE_TO_LOG_FILE;
}
